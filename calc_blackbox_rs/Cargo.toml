[package]
name = "calc_blackbox"
version = "1.0.0"
edition = "2021"
description = "ADV9 C++ 計算黑盒 — Rust 重寫版"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 3
lto = true
strip = true
